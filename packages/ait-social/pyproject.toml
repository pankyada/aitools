[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "ait-social"
version = "1.0.0"
description = "Social media CLI tools for Instagram, Facebook, Twitter, LinkedIn, and TikTok"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
  "ait-core>=1.0.0",
  "httpx>=0.28.1",
  "pydantic>=2.10.6",
  "typer>=0.15.2",
  "rich>=13.9.4",
]

[project.scripts]
ait-social = "ait_social.cli:app"

[project.entry-points."ait.tools"]
social = "ait_social.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/ait_social"]
